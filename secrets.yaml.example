# CPG2PVG-AI Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit the actual .env file to version control

# ==============================================================================
# SECURITY WARNING: These are template values only!
# Replace all placeholder values with strong, unique values for production.
# Use a password manager to generate secure credentials.
# ==============================================================================

# Database Configuration
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_REPLICATION_PASSWORD=your_secure_replication_password_here

# Redis Configuration
REDIS_PASSWORD=your_secure_redis_password_here

# MinIO Configuration
MINIO_ROOT_USER=your_minio_access_key_here
MINIO_ROOT_PASSWORD=your_minio_secret_key_here
MINIO_ACCESS_KEY=your_minio_access_key_here
MINIO_SECRET_KEY=your_minio_secret_key_here

# Application Security
SECRET_KEY=your_app_secret_key_minimum_32_characters_long
JWT_SECRET_KEY=your_jwt_secret_key_minimum_32_characters_long

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# External Services
GOOGLE_ANALYTICS_ID=your_google_analytics_id_here
SENTRY_DSN=your_sentry_dsn_here

# Email/SMTP Configuration (for notifications)
SMTP_USERNAME=your_smtp_username_here
SMTP_PASSWORD=your_smtp_password_here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Flower Authentication
FLOWER_USER=flower_admin_username
FLOWER_PASSWORD=flower_admin_secure_password_here

# SSL/TLS Certificates (if using self-signed)
TLS_CERT_PATH=/path/to/your/tls/certificate.crt
TLS_KEY_PATH=/path/to/your/tls/private.key

# MinIO TLS Certificates
MINIO_TLS_CERT_PATH=/path/to/minio/certificate.crt
MINIO_TLS_KEY_PATH=/path/to/minio/private.key

# External API Keys
EXTERNAL_API_KEY=your_external_service_api_key_here

# ==============================================================================
# Password Generation Commands (use these to generate strong passwords):
#
# For general passwords:
# openssl rand -base64 32
#
# For JWT secrets:
# openssl rand -hex 64
#
# For MinIO keys (must be at least 3 characters):
# openssl rand -base64 16 | tr -d "=+/" | cut -c1-16
#
# For database passwords (special characters allowed):
# openssl rand -base64 32 | tr -d "\n"
# ==============================================================================